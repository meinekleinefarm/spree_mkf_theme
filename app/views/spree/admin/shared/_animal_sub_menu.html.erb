<% content_for :sub_menu do %>
  <ul id="sub_nav" data-hook="admin_animal_sub_tabs" class="inline-menu">
    <%= tab :schweine, :match_path => '/schweine' %>
    <%= tab :rinder, :match_path => '/rinder' %>
    <%= tab :schafe, :match_path => '/schafe' %>
  </ul>
<% end %>